package test;

import java.util.Arrays;
import java.util.List;
import java.util.stream.Collectors;

public class Test2 {

	public static void main(String[] args) {

		//Integer[] arr2 = { 1, 2, 35, 4, 5, 6, 75, 9 };
		//int evenmax = Arrays.stream(arr).filter(item -> item %2 == 0).max().getAsInt();
		//int oddmin = Arrays.stream(arr).filter(item -> item %2 != 0).min().getAsInt();
		//System.out.println(evenmax);
		//System.out.println(oddmin);
		
		/*
		 * List<Integer> list = Arrays.stream(arr2).filter(item ->
		 * item.toString().contains("5")).collect(Collectors.toList());
		 * System.out.println(list);
		 * 
		 * 
		 * int[] arr = new int[] { 10, 11, 12, 13, 23, 25, 40, 45, 56, 13 }; int count =
		 * 0; for (int i = 0; i < arr.length; i++) { int num = arr[i]; if (num >= 10 &&
		 * num <= 20) { System.out.println("number between 10 to 20 : " + num); count++;
		 * if (count > 0) { System.out.println("The no of occurences of" + num + ":" +
		 * count); } } }
		 */
		
		
		
		
		

}
}
